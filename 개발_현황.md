# 여울 SSH 클라이언트 — 개발 현황 및 다음 단계

**프로젝트 코드:** YSC-2026-001
**작성일:** 2026-09-20 (연구팀 결정사항 3건 반영 완료)
**작성:** 개발팀 → 연구팀
**현재 단계:** 주 1~2 (아키텍처 확정 + V1.0 코어 구현 완료, 빌드·테스트 대기)

---

## 1. 지금 바로 할 일 (3단계)

```
1) Node.js 20 LTS 설치         → BUILD.md 0장 참고 (winget install OpenJS.NodeJS.LTS)
2) 이 폴더에서  npm install     → Electron 등 의존성 내려받기 (처음 한 번 몇 분)
3) npm run verify  후  npm start → 검증 통과 확인 후 프로그램 실행
4) npm run dist:win             → 설치 파일 생성
5) npm run sign                 → 자체 서명 (openssl 필요)
6) npm run hash                 → SHA256SUMS.txt
```

> 5번에는 `openssl` 이 필요합니다. 없으면 `winget install ShiningLight.OpenSSL.Light`
> 또는 이미 설치된 Git for Windows 의 openssl 을 PATH 에 추가하세요.

실행되면 여울 게임터 머드 서버로 접속해 한글 입출력부터 확인해 주세요.
배포 파일(exe / AppImage / DEB / RPM / Snap)은 `npm run dist:win`, `npm run dist:linux` 입니다.

---

## 2. 기술 스택 확정 (RFP 5.1 "SSH 라이브러리 선택" 답변)

| 영역 | 선택 | 선택 이유 |
|------|------|----------|
| 런타임 | **Electron 31** (Chromium + Node 20) | Windows·Linux 단일 코드베이스, exe/AppImage/DEB/RPM/Snap 을 electron-builder 로 한 번에 생성 |
| 터미널 | **xterm.js 5 + unicode11 addon** | xterm 호환 에뮬레이션·마우스·스크롤백이 검증되어 있고, **한글/CJK 문자폭 2칸 계산**이 정확 |
| SSH | **ssh2 1.15** | 순수 JavaScript SSH-2 구현, 네이티브 빌드 불필요 → 배포판별 컴파일 문제 없음 |
| 인코딩 | **iconv-lite(CP949) + Node StringDecoder** | EUC-KR 레거시 서버 대응, 청크 경계 분할 처리 |
| i18n | 자체 경량 로더 (JSON + en 폴백) | 의존성 추가 없이 5개 국어 + 부분 번역 폴백 요구사항 충족 |
| 난독화 | javascript-obfuscator | RFP 3.1.1 |

**후보 비교:** Python+PyQt 는 터미널 에뮬레이션을 직접 구현해야 해 한글 폭 계산·IME 위험이 크고,
C#/WPF 는 Linux GUI 요구사항(AppImage/DEB/RPM)을 충족하기 어려워 제외했습니다.

---

## 3. RFP 요구사항 대비 구현 현황

### 2.1 필수 기능 (V1.0)

| RFP 항목 | 상태 | 구현 위치 / 비고 |
|---------|------|-----------------|
| SSH2 프로토콜 | ✅ | `ssh-manager.js` |
| 안정적 연결 / Keep-alive 60초 | ✅ | `keepaliveInterval` 기본 60초, countMax 5 |
| 비활성 중 자동 재연결 | ✅ | 지수 백오프(2→30초), 최대 5회, 사용자가 끊으면 재시도 안 함 |
| 타임아웃 설정 (기본 없음) | ✅ | 설정 > 연결, 0 = 무제한 |
| xterm 호환 에뮬레이션 | ✅ | `term: xterm-256color` |
| 마우스 지원 / 스크롤백 1000줄 | ✅ | xterm.js 기본 + 설정 |
| **한글 UTF-8 자동 감지** | ✅ | `encoding.js` — 1바이트 단위 분할에서도 복원 확인 |
| **한글 입력 100%** | ✅ | xterm.js IME 조합 처리, 입력도 세션 인코딩으로 변환 |
| **한글 출력 깨짐 없음** | ✅ | 청크 경계 멀티바이트 보존 (`scripts/test-encoding.js` 23건 통과) |
| **한글 복사/붙여넣기** | ✅ | `Ctrl+Shift+C/V`, 선택 시 자동 복사·우클릭 붙여넣기 옵션 |
| 한글 폰트 선택 | ✅ | D2Coding / 나눔고딕코딩 / Noto Sans Mono CJK KR 등, 글리프 부재 시 경고 |
| EUC-KR / UTF-8 자동 감지 | ✅ | auto 모드 + 세션별 수동 지정 |
| 다중 탭 (추가/삭제/이름/전환) | ✅ | `app.js`, 더블클릭으로 탭 이름 변경, Ctrl+Tab 전환 |
| 세션 저장/로드/삭제/복사 | ✅ | 사이드 패널, 클릭 한 번으로 접속 |
| 세션 내보내기/가져오기 | ✅ | JSON, **비밀번호는 제외** |
| 세션별 설정 (색상/아이콘/인코딩) | ✅ | 연결 대화상자 고급 설정 |
| 다크/라이트 테마, 색상·폰트·크기 | ✅ | 설정 > 모양, Ctrl +/-/0 |
| 메뉴바 + 버튼 바 | ✅ | 7개 메뉴 + 빠른 명령어 바 |
| SSH 키 / 비밀번호 / 에이전트 인증 | ✅ | PEM·OpenSSH 키 파일 선택 |
| **자격증명 암호화 보관** | ✅ | OS 키체인 우선, 없으면 마스터 비밀번호(AES-256-GCM). **평문 저장 없음** |
| 마스터 비밀번호 (옵션) | ✅ | scrypt(N=16384) + AES-256-GCM |
| 세션 종료 시 정리 | ✅ | 창 닫기 시 전체 연결 해제, 종료 확인 |
| 한국어·영어 완전 지원 | ✅ | 메뉴 + 메시지 + 도움말 240개 키 |
| 일본어·중국어·독일어 UI | ✅ | 메뉴·버튼 168개 키, 나머지 영어 폴백 |
| 자동 언어 감지 / 수동 선택 | ✅ | Windows 로케일, Linux `LC_ALL`/`LANG`, 메뉴 > 언어 |
| 복사/붙여넣기/연결 해제/새로고침/상태 표시 | ✅ | 메뉴 + 상태 표시줄 |

### 2.2 V1.1 항목 중 선반영

| 항목 | 상태 |
|------|------|
| 빠른 명령어 버튼 (6종 기본 + 편집) | ✅ **V1.0 에 선반영** (검수 항목 5.1.3 에 포함되어 있어 미리 구현) |
| 세션별 색상·아이콘 | ✅ 선반영 |
| 상태 표시줄 (연결 시간, 명령어 수) | ✅ 선반영 |
| 로그 저장 / 검색 | ⏳ V1.1 |
| 포트포워딩 | ⏳ V1.1 |

### 3. 보안 & 지적재산권

| RFP 항목 | 상태 | 비고 |
|---------|------|------|
| 코드 난독화 | 🔧 스크립트 준비 완료 | `npm run dist:secure` — 난독화 → 패키징 → 서명 → 해시 일괄 실행 |
| 실행 파일 서명 | ✅ **자체 서명 구현 완료** | `npm run sign` — 인증서 자동 생성, detached 서명(.sig), 자가 검증, 사용자 검증 가이드 생성 |
| 공개 인증서 지문 게시 | ⏳ 배포 시 필수 | 게시하지 않으면 자체 서명이 무의미해집니다 |
| UPX 압축 / AppImage | ✅ | asar + NSIS 압축, AppImage squashfs |
| SHA-256 해시 | ✅ | `npm run hash` → `SHA256SUMS.txt` |
| 자동 업데이트 체크 | ✅ | `api.yeoulgame.com/ssh/version`, **자동 설치 없음**(사용자 동의) |
| 무결성/비공식 배포처 경고 | ✅ | 버전 비교 안내 |
| MIT 라이선스 + LICENSE 파일 | ✅ | |
| **모든 소스 최상단 저작권 표시** | ✅ | `check-syntax.js` 가 자동 검사 (25개 파일 통과) |
| 시작 화면 저작권 표시 | ✅ | 환영 화면 하단 |
| About 창 (로고·버전·링크) | ✅ | 별도 창, 여울게임터/GitHub/라이선스 버튼 |
| 도움말 메뉴 저작권 | ✅ | |
| 설치 마법사 개발처 표시 | ✅ | NSIS 다국어 설치 마법사 + LICENSE 동의 |
| Windows 속성 > 세부 정보 | ✅ | 제품명·회사·저작권·상표 메타데이터 |
| GitHub README / CONTRIBUTING | ✅ | |

### 6. 호환성

| 항목 | 상태 |
|------|------|
| Windows 10 (1909/21H2/22H2) | 🔧 빌드 후 테스트 필요 |
| Windows 11 (21H2/22H2/23H2) | 🔧 빌드 후 테스트 필요 |
| Ubuntu 20.04/22.04/24.04, Debian 11/12, CentOS 9 Stream, Fedora 37~39 | 🔧 빌드 후 테스트 필요 |
| AppImage / DEB / RPM / Snap 타깃 설정 | ✅ `package.json` build 섹션 |
| CentOS 7 / 8 | ➖ **지원 제외 확정** (연구팀 결정, 2026-09-20) |

---

## 4. 자동 검증 (현재 전부 통과)

```
npm run verify
```

| 스크립트 | 검사 내용 | 결과 |
|---------|----------|------|
| `verify-i18n.js` | ko/en 240키 완전 일치, ja/zh-CN/de UI 168키, 치환자 일치 | ✅ 오류 0 |
| `check-syntax.js` | 25개 소스 문법 + **저작권 헤더 전수 검사** | ✅ 오류 0 |
| `test-encoding.js` | 한글 인코딩 23건 (1바이트 분할, EUC-KR 자동감지, 이모지, ANSI, 입력 인코딩) | ✅ 23/23 |
| `test-store.js` | 자격증명 암호화 22건 (평문 저장 거부, 재시작 복호화, 내보내기 제외) | ✅ 22/22 |

---

## 5. 연구팀 결정사항 3건 — 반영 완료

2026-09-20 연구팀 회신(`[YSC-2026-001] 검수 이슈 3건 - 최종 결정`)에 따라 모두 처리했습니다.

### (1) CentOS 7 지원 제외 ✅

| 조치 | 반영 위치 |
|------|----------|
| 지원 목록에서 CentOS 7·8 제거, CentOS 9 Stream 만 유지 | `COMPATIBILITY_REPORT.md` 2장 |
| 설치 가이드에 미지원 사유 명시 | `INSTALL.md` 2장 방식 3, 6장 문제 해결 |
| 빌드 타깃 영향 | **없음** — RPM 은 CentOS 9 / Fedora 공용이라 타깃 변경 불필요 |

> CentOS 8 도 2021-12 EOL 이라 함께 제외했습니다. 이견 있으시면 알려주세요.
> RFP 6.2.1 본문 수정은 연구팀 문서라 개발팀이 건드리지 않았습니다. `변경사항.txt` 에 정리해 뒀습니다.

### (2) OpenSSL 자체 서명 ✅

`scripts/sign-release.js` 신규 작성, `npm run sign` 으로 실행합니다.

| 기능 | 내용 |
|------|------|
| 인증서 자동 생성 | RSA 3072, SHA-256, `codeSigning` EKU, 유효기간 10년 |
| detached 서명 | 모든 배포 파일에 `.sig` 생성 후 **즉시 자가 검증** |
| Windows Authenticode | signtool 있으면 .exe 에 자동 적용, 없으면 건너뜀 |
| 공개 인증서 배포 | `release/yeoulgame-public-cert.crt` |
| 사용자 검증 가이드 | `release/SIGNATURE.md` 자동 생성 (지문·해시표·명령어) |
| 파이프라인 연결 | `dist:secure`, `build-win.ps1`, `build-linux.sh` 에 기본 포함 |

개발 환경에서 서명·검증·**변조 탐지**까지 실제로 돌려 확인했습니다
(파일 1바이트만 바뀌어도 `Verification failure` 로 거부).

**⚠ 연구팀이 꼭 알아야 할 점 2가지**

1. 자체 서명은 **SmartScreen 경고를 없애지 못합니다.** 오히려 "알 수 없는 게시자"에서
   "신뢰할 수 없는 인증서"로 문구가 바뀔 수 있습니다. 결정문에서 수용하신 대로 진행했습니다.
2. 자체 서명의 유일한 실효는 **사용자가 공개 인증서 지문을 공식 배포처와 대조할 때** 생깁니다.
   지문을 `yeoulgame.com/downloads` 와 GitHub Release 양쪽에 게시하지 않으면 서명이 무의미합니다.
   배포 체크리스트에 넣어 두었습니다.

**개인키 관리:** `signing/` 폴더는 `.gitignore` 에 등록했습니다. 유출되면 제3자가 여울게임터
이름으로 서명할 수 있고, 분실하면 기존 사용자가 새 배포판을 검증할 수 없으니 오프라인 백업이 필요합니다.

### (3) 임시 아이콘 유지 ✅

- `build/icon.ico`, `build/icons/*.png` 그대로 유지
- About 창 상단에 교체 대상과 시점을 적은 주석 추가 (`src/renderer/about.html`)
- 정식 로고 준비되면 아이콘 파일 교체 + `.welcome-logo` 영역만 수정하면 됩니다

### 추가 반영 — About 창 서명 상태 표시

결정문 2번의 선택 항목이었지만 사용자 보호에 직접 도움이 되어 구현했습니다.

> **이 프로그램은 자체 서명되었습니다**
> 공인 인증기관(CA)이 아닌 여울게임터가 직접 서명했기 때문에 Windows에서 게시자 확인 경고가
> 표시될 수 있습니다. 기능에는 영향이 없습니다. 반드시 공식 배포처에서만 내려받고,
> 설치 전 SHA-256 해시와 서명을 확인하세요. → [해시·서명 검증 방법 보기]

### 추가 반영 — 배포 파일명을 RFP 체크리스트와 일치

| 이전 | 변경 후 | 근거 |
|------|--------|------|
| `...-zip.zip` | `yeoulgame-ssh-client-1.0.0-portable.zip` | RFP 배포 파일 체크리스트 |
| `yeoulgame-ssh-client_1.0.0_amd64.snap` | `yeoulgame-ssh-client.snap` | RFP 배포 파일 체크리스트 |

---

## 6. 일정 대비 현황

| 날짜 | 마일스톤 | 상태 |
|------|---------|------|
| 2026-09-20 | RFP 접수 · 아키텍처 확정 · **V1.0 코어 구현** | ✅ 완료 |
| 2026-09-27 | 빌드 · 기능 테스트 · 버그 수정 | ⏳ 진행 예정 |
| 2026-10-04 | 호환성/보안 테스트 · 연구팀 제출 | 예정 |
| 2026-10-11 | 승인 · 공식 배포 | 예정 |
| 2026-10-18 | V1.1 (로그 저장, 포트포워딩) | 예정 |

---

## 7. 제출 서류 준비 상태

| 문서 | 상태 |
|------|------|
| `README.md` (한/영) | ✅ |
| `INSTALL.md` | ✅ |
| `BUILD.md` (Node.js 설치 포함) | ✅ |
| `CONTRIBUTING.md` | ✅ |
| `LICENSE` (MIT) | ✅ |
| `COMPATIBILITY_REPORT.md` | 🔧 템플릿 완성 (CentOS 7 제외 반영), 테스트 결과 기입 대기 |
| `SECURITY_REPORT.md` | 🔧 자체 서명 방식 반영 완료, 빌드 결과 기입 대기 |
| `변경사항.txt` | ✅ 제출용 변경 요약 작성 완료 |
| `release/SIGNATURE.md` | 🔧 서명 시 자동 생성 |
| `APPROVAL.md` | ⏳ 연구팀 승인 후 |

제출처: research@yeoulgame.com · 제목 `[YSC-2026-001] V1.0 제출 - <날짜>`
